__pycache__/
*.pyc
.venv/
.env
logs/*.jsonl
logs/*.log
logs/*.pid
logs/*.json
logs/*.err
logs/*.out
logs/_*
scripts/_*
*.egg-info/
dist/
build/
.pytest_cache/

# Plan 5b: attachments handled by Obsidian Sync, not git
tasks/_attachments/

# Local dogfood / scratch artifacts (not deployment outputs).
xiaomi_*.html
xiaomi_*.json
.claude/

# Scratch / probe outputs (curl dumps, healthz responses, etc.)
.tmp_*

# Obsidian per-user state — let each user generate their own when they
# first open tasks/ as a vault. We don't ship baseline config.
tasks/.obsidian/
